<UserControl
    x:Class="PacMan.App.Views.StartPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:viewmodels="using:PacMan.App.ViewModels"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d">

    <Grid Background="Black" Padding="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Spacing="8">
            <TextBlock Text="PAC-MAN" FontSize="52" FontWeight="Bold" Foreground="Yellow"/>
            <TextBlock Text="Pressione START para jogar" Foreground="White" FontSize="16" HorizontalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Spacing="12" Margin="0,20,0,20">
            <Button x:Name="StartButton" Content="Start" Padding="20,8"/>
            <Button Content="{Binding MuteLabel}" Command="{Binding ToggleMuteCommand}" Padding="20,8"/>
            <Button x:Name="ExitButton" Content="Sair" Padding="20,8"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Background="#222" Padding="16" Width="360" HorizontalAlignment="Center">
            <TextBlock Text="HIGH SCORES" Foreground="Yellow" FontSize="20" HorizontalAlignment="Center" Margin="0,0,0,12"/>
            <ListView ItemsSource="{Binding HighScores}" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding PlayerName}" Foreground="White" FontSize="14"/>
                            <TextBlock Text="{Binding Score}" Grid.Column="1" Foreground="Cyan" FontWeight="Bold"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</UserControl>
